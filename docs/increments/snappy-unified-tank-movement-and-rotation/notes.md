use interface to seperate ebiten input management from test management

extract intent driven to ADR

remove fallback logic, adjust Constitution to state: no backward compatibility, fail fast instead and no fallbacks 

